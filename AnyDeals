#!/usr/bin/env python3
"""
Anydeals Agent CLI launcher.

This wrapper should behave like the installed `anydeals` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from AnyDeals_cli.main import main
    main()
